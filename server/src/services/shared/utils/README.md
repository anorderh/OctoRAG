# Jobs

These are functions I implemented in Typescript on my monolothic server that if used in a real application, should instead be deployed as serverless, isolated compute resources to encourage scalability.
